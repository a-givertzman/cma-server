## 20:03

### Все последние изменения сделанные в `Tests-BasicIntegration` затянул в `wear-services`.
    - Фиксы `FnAverage`.
    - Исправленные тестовые данные в `tables_and_configs/rec-basic-metric/rec-basic-metric-test.ods`.
    - Поправлен конфиг `tables_and_configs/rec-basic-metric/rec-basic-metric.yaml`.

### Актуальная версия проекта зафиксирована тэгом `0.3.2` и в данный момент находится в ветке `wear-services`.

### А так же все обновления затянул из `wear-services` в `Tests-BasicIntegration`

На данный момент `wear-services` и `Tests-BasicIntegration` синхронизированы.

#### 1. FRDM: Доработан алгоритм износа каната (`RopeDeprecation`)
- Добавлен `BlockBind::Fixed` — привязка статических блоков.
- Добавлены тесты на данных «**SPU TNPA Optimist**» (реальный объект).
- Переработан и исправлен расчёт износа, рефакторинг общей структуры алгоритма, включая оптимизацию производительности.
- Дополнены доки в `design/frdm-service/spu-tnpa-optimist/`.
- Написан `README.md` с описанием алгоритма, обновлен SQL (`sql.md`).

#### 2. Сервис `VibroMonitor` (новый)
- Создан сервис `src/services/vibro_monitor/`: конфигурация (`vibro_monitor_conf`), таблицы, экспорт в SQL.
- Новый `udp_client` с поддержкой разных типов входных данных (`input_type`), конфигурация и connect-логика.
- Модуль `vibro_adc` — обработка данных АЦП; первичный анализ датчиков (`sensor_conf`).
- Тесты: `udp_client_test`, `udp_client_conf_test`, mock UDP-сервера.
- Пример конфига `vibro-monitor.yaml`.

#### 4. Сопутствующие изменения
- `DebugSession` — обновлён и исправлен.
- Обновлены конфиги и документация по итогам отладки (`FRDM | Deprecation | Conf, Tests, Docs fixes`).
- Через merge из `Service-ModbusTcp` интегрирован сервис ModbusTcp (парсинг сообщений, read/write-подсервисы, `ModbusUnit`).

---
